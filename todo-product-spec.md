# todo — Android 产品规格说明书

**版本：** V1.0  
**日期：** 2026-09-10  
**用途：** 产品设计、Android 实现与验收基线  
**产品名称：** `todo`，始终使用小写

> 一个只在 Android 上使用的本地待办工具：随手收集，以标签组织，为任务安排时间、设置截止与提醒，并在需要时通过“结束一天”完成收尾。

本文中未由用户逐项指定的交互与技术细节，是为使 V1 可直接实施而选定的默认方案；不应把这些默认方案扩展成更多设置或产品功能。

---

## 1. 定位与边界

### 1.1 产品目标

让用户用尽可能少的操作完成四件事：记下一件事；在需要时给它贴标签和安排时间；在合适的时间看到或收到提醒；完成后放下，或在一天结束时重新安排。

**todo 是待办工具，不是时间管理课程、项目管理平台或个人生产力操作系统。**

使用不依赖账号、网络、服务器、其他设备或每天必须完成的流程。打开即可使用，没有“开始一天”。“结束一天”是用户主动选择的收尾仪式，而不是正常使用的前置条件。

### 1.2 V1 范围

| 纳入 V1 | 明确不做 |
|---|---|
| 任务新增、编辑、完成、恢复、删除 | 项目、文件夹、工作区、子任务、依赖关系 |
| 收件箱、标签、多标签 | 优先级矩阵、看板、自定义复杂筛选器 |
| 安排日期/时间、截止日期/时间 | 预计耗时、时间轴排程、时间盒、计时、番茄钟 |
| 单任务一次性本地提醒 | 重复任务、习惯打卡、位置提醒 |
| 今天视图、未来任务的次级列表 | 完整日历、系统日历/邮箱集成 |
| 可选的“结束一天” | 开始一天、强制晨间规划、每日强制清零 |
| 基础搜索、已完成记录 | 效率评分、连续打卡、统计仪表盘、AI |
| 浅色/深色、必要的无障碍支持 | 主题商店、插件系统、自定义布局 |
| 简单的手动本地备份与恢复 | 云备份服务、多端同步、账号、协作 |

V1 以 Android 手机为主要形态，默认兼容 Android 10 及以上。横屏与较大窗口不得出现遮挡，但不单独建设平板工作台。源码随项目交付；不依赖私有服务。

### 1.3 优先级

冲突时遵循：**不丢数据 > 时间和提醒语义正确 > 操作直接 > 界面清晰 > 动画精致。**

动画、视觉效果与仪式感不得阻挡任务操作，也不得改变任务的真实状态。

---

## 2. 核心概念

| 概念 | 产品定义 |
|---|---|
| 任务 Task | 一件待处理的事情，只有标题必填。 |
| 收件箱 Inbox | 所有没有安排日期、且尚未完成的任务。它是自动生成的视图，不是文件夹。 |
| 标签 Tag | 任务的唯一自定义组织机制。一个任务可有零个或多个标签。 |
| 安排时间 Schedule | “我打算什么时候做”，可只有日期，也可精确到分钟。 |
| 截止时间 Due | “最晚什么时候完成”，可只有日期，也可精确到分钟。 |
| 提醒 Reminder | 一次明确的本地通知，可跟随安排时间、跟随截止时间，或指定独立时间。 |
| 今天 Today | 今天及此前安排的未完成任务，以及今天到期或已经逾期的未完成任务。 |
| 结束一天 End Day | 回看今天完成的事情，可选地调整未完成任务，然后留下一个收尾状态。 |

### 2.1 三种时间必须分开

例如，“提交报告”可以安排在 9 月 10 日 19:00 做，截止于 9 月 12 日 18:00，并在 9 月 10 日 18:50 提醒。

这三个字段互不替代。改变安排时间不改变截止时间；完成提醒不等于完成任务；超过安排时间不代表超过截止时间。

### 2.2 收件箱与标签

带标签的任务不再出现在收件箱。收件箱只包含「无安排日期 且 无标签」的任务；为任务设置截止日期不会让它离开收件箱，只有设置安排日期或给它打上标签，才会使它不再匹配收件箱。

同一个任务可以同时出现在收件箱、今天、清单（按标签 / 全部 / 已完成）中，它们指向同一条记录。任一视图中的编辑、完成和删除必须立即反映到其他视图。

### 2.3 不设置独立的“今日任务副本”

“安排到今天”就是把任务的安排日期设为今天。不维护另一份 `today_task_ids`，也不复制任务生成每日清单。一天自然变化时重新计算视图，不批量改写任务日期。

---

## 3. 时间与视图规则

### 3.1 日期与时刻

所有用户输入的日期/时间，在 V1 中都采用**手机所在地的本地时间**语义。日期和可选时间分开存储，不能把“只有日期”伪装成“当天 00:00”。

设备更换时区后，任务仍显示用户输入的本地日期和钟点，并重新计算提醒。例如 09:00 仍表示所在地 09:00。V1 不提供固定其他时区的会议时间；这是明确的产品取舍。

“今天”按手机本地日历日定义，在午夜自然切换，不依赖 Start Day 或 End Day。应用从后台恢复时也必须重新计算日期。

只有截止日期的任务，在该日期整个自然日内都不算逾期；进入下一天才算逾期。有具体截止时间的任务，到该时刻后尚未完成即算逾期。

安排日期已过去的任务显示“此前安排”，不因此使用“已逾期”字样。

### 3.2 视图筛选

设 D 为当前本地日期，活动任务指尚未完成且未被删除的任务。

| 视图 | 纳入规则 |
|---|---|
| 收件箱 | 活动任务，且没有安排日期。是否有标签、截止、提醒均不影响。 |
| 今天 | 活动任务，且“安排日期 ≤ D”或“截止日期 ≤ D”。 |
| 之后 | 不在今天视图中的活动任务，且存在未来安排日期或未来截止日期。 |
| 某标签 | 具有该标签的全部活动任务。 |
| 全部待办 | 全部活动任务，不受日期和标签限制。 |
| 无标签 | 没有标签的全部活动任务。 |
| 已完成 | 已完成且未被删除的任务。 |

每个列表内部按任务 ID 去重。未安排但有未来截止日期的任务，可以同时位于收件箱与“之后”。

### 3.3 今天的分组与排序

同一任务只落入以下最先匹配的一组：

| 分组 | 规则 | 排序 |
|---|---|---|
| 已过截止 | 已超过截止日期/时刻，仍未完成 | 截止时间从早到晚 |
| 此前安排 | 未逾期，但安排日期早于今天 | 原安排日期从早到晚 |
| 今天 | 剩余匹配今天视图的任务 | 今天的具体安排时间优先；没有时采用今天的具体截止时间；无具体时间放后 |

同一排序键下以创建时间、任务 ID 稳定排序。只有有内容的分组才显示标题。过去一天的已完成任务不继续占据今天页面。

其他默认排序：收件箱按最近创建优先；“之后”按下一次未来安排/截止日期分组；标签详情按安排日期升序、未安排放后，同日按创建时间稳定排序；已完成按最近完成优先。V1 不做手动拖拽排序。

### 3.4 冲突处理

安排时间晚于截止时间时，允许保存，但在编辑界面与任务详情中显示“安排晚于截止”。应用不得为了消除冲突而自动延后截止时间。

对于只有日期的时间，比较到日期粒度；不能推断未填写的钟点。同一天的“安排 20:00、仅日期截止”不构成冲突。

### 3.5 示例

| 任务状态 | 应出现的位置 |
|---|---|
| 无安排、无截止、有标签 | 收件箱、标签详情、全部待办 |
| 明天安排，无标签 | 之后、无标签、全部待办 |
| 今天截止但未安排 | 今天、收件箱、全部待办 |
| 昨天安排，截止在后天 | 今天的“此前安排”、对应标签 |
| 昨天截止，却安排在明天 | 今天的“已过截止”；详情保留冲突提示 |
| 已完成 | 已完成记录；若今天完成，也出现在今天的折叠完成区 |

---

## 4. 信息架构

主导航固定为三项，顺序固定：**今天 / 收件箱 / 清单**。

```text
今天（默认首页）
  ├─ 今日任务
  ├─ 今日完成（折叠）
  ├─ 之后（次级页面）
  └─ 结束一天（主动打开的收尾页面）
收件箱
清单
  ├─ 搜索
  ├─ 全部（Tab）
  ├─ 已完成（Tab）
  └─ 标签筛选条（各标签 / 无标签）
公共次级入口
  └─ 设置
```

“之后”只是按日期分组的任务列表，不是第四个主导航，也不是月历。“结束一天”不进入底部导航。搜索只出现在“清单”页内。

顶部只保留当前页面标题和少量动作。所有主页面有稳定可见的添加按钮；不能以长按或滑动作为新增、完成、编辑、安排任务的唯一入口。

---

## 5. 主页面设计

### 5.1 今天

进入 App 后直接显示今天。顶部为“今天”和当前日期，不显示效率分数、励志标语或巨大的统计卡片。

主体为按第 3 节规则生成的任务列表，下方为可折叠的“今天完成 N 项”。“结束一天”以低强调度按钮放在日期标题附近，始终可发现，不根据晚上几点突然改变布局。

空状态文案：“今天没有安排。”次级入口为“添加任务”和“查看收件箱”。不能因为 Today 为空就暗示所有任务已经完成。

当天已经收尾时，日期附近显示轻量的“今天已收尾”。不隐藏未完成任务，不锁定界面；仍可新增、完成和改期。

### 5.2 收件箱

用于记录尚未安排到哪一天、且尚未打标签的事情。首次进入时，用一行可消失的说明解释：“还没决定哪天做的事，先放这里。”

列表显示任务标题，以及必要的截止/提醒信息。给任务加标签或安排到某一天后，它离开收件箱，平滑移出当前列表。

空状态：“还没有未安排且未打标签的事。”不显示“任务全部完成”，因为其他视图可能仍有待办。

### 5.3 标签

标签在“清单”页以筛选条（chip）呈现，与“全部 / 已完成”Tab 并列：默认显示全部标签，点击某个标签筛选到该标签的任务，“无标签”筛选未打标签的任务；再点一次取消筛选。标签的创建、改名与删除入口在清单页顶栏溢出菜单与“新建标签”chip。

标签只有名称，不提供层级、描述、图标或用户自选颜色。名称去除首尾空格后，按不区分大小写的规则保持唯一；同名创建直接复用已有标签。

支持创建、改名和删除。删除标签前明确提示“只移除标签，任务会保留”。标签删除不得删除任务，也不得清除安排、截止或提醒。

标签选择使用底部面板，支持名称搜索、多选和“创建这个标签”。任务列表中最多展示两个标签，其余显示“+N”；详情展示完整标签。

---

## 6. 任务交互

### 6.1 快速添加

点击添加按钮后打开底部输入面板，自动聚焦标题并弹出键盘。标题是唯一必填项，输入后可以直接保存。

第一层只显示标题、紧凑的标签/安排入口和保存按钮。截止、提醒、备注放在展开区域，不把新增流程做成多步向导。

| 进入位置 | 初始值 |
|---|---|
| 今天 | 预选“安排：今天”，以可移除标签明确显示 |
| 收件箱、全部待办、无标签 | 不设置安排、截止、提醒和标签 |
| 某个标签详情 | 预选当前标签，不设置安排日期 |
| 之后 | 不猜测日期，由用户明确选择 |

从今天创建任务时，可以移除“今天”从而将任务放入收件箱。默认值必须可见，不允许根据用户看不见的规则设置截止或提醒。

保存后关闭面板并反馈成功；连续点保存只能创建一条任务。标题为空或全是空格时不能保存。标题上限 300 字符，备注上限 5,000 字符，标签名称上限 30 字符。

未保存且已输入内容时，返回或下拉关闭应确认是否放弃；纯空表单可以直接关闭。编辑表单在旋转、临时切后台和进程重建后恢复草稿，不生成重复任务。

### 6.2 任务行

左侧为完成控件，中间为标题，次行为必要元信息。点完成控件只切换完成状态，点任务正文打开详情；两块触控区域不得互相抢占。

标题最多显示两行，详情显示全文。元信息只出现真实存在的内容，不能为每个空字段显示占位图标。不同时间使用文字区分，例如“安排 今天 19:00”“截止 周五”。

重要的截止信息不得因标签过多被挤掉。逾期同时使用文字与颜色表达，不能只靠红色。

### 6.3 详情与编辑

任务详情为一个可直接编辑的页面，顺序固定：标题、备注、标签、安排、截止、提醒。每个时间字段都可以单独清除。

字段修改先保留为表单草稿，点击“保存”统一提交。退出存在未保存修改的页面时，提供“保存 / 放弃 / 继续编辑”，不得一部分字段自动提交、另一部分还在草稿。

### 6.4 完成、恢复与删除

完成是一次点击操作，写入完成时间与完成当时的本地日期，取消未来提醒和已显示的对应通知，随后移动到已完成区。短暂显示“已完成 · 撤销”。

从已完成恢复后，根据原安排和截止重新进入对应视图。仍在未来的提醒可以恢复；过去的提醒不因恢复任务而突然补发。

删除位于详情的次级菜单，必须二次确认。V1 不做回收站；确认后删除任务、标签关联与提醒记录，标签本身保留。**结束一天流程不提供批量删除。**

### 6.5 搜索与已完成记录

搜索只在“清单”页内，对标题、备注、标签名称做本地文本搜索。搜索与“全部 / 已完成”Tab 及标签筛选条叠加，作用在当前 Tab 与标签范围内。

任务出现在多个标签时，搜索结果仍只出现一次。无结果显示“没有找到相关任务”，不是加载失败。

已完成在“清单”页的“已完成”Tab 中，按完成时间倒序显示，可查看详情和恢复任务，不做图表、连续记录或排行榜。

---

## 7. 安排、截止与提醒选择器

安排和截止使用一致的日期/时间面板，但标题与说明清楚区分：“安排：打算什么时候做”“截止：最晚什么时候完成”。

安排提供“今天、明天、选择日期、取消安排”。具体钟点是可选项；选中日期后才显示“添加时间”。截止使用同类控件，但不能默认复制安排日期。

提醒默认关闭。V1 每个任务最多一个有效提醒：

| 模式 | 规则 |
|---|---|
| 不提醒 | 不建立系统提醒 |
| 按安排时间 | 仅在安排包含具体钟点时可用，跟随安排变化 |
| 按截止时间 | 仅在截止包含具体钟点时可用，跟随截止变化 |
| 自定义 | 用户选择独立的日期与具体钟点，不随安排/截止移动 |

只有日期的任务也能设置提醒，但须使用自定义提醒明确选择钟点，不能暗中设定午夜或早上 9 点。

取消安排会取消依赖安排的提醒，保存前明确说明；截止提醒与自定义提醒不受影响。取消截止同理。改期时若存在不随之变化的自定义提醒，显示其保留的具体时间。

用户新设的提醒时间必须在未来。允许保存过去的安排/截止，但不能把过去的时间作为一个新提醒静默立即触发。跟随时间的已有提醒被移到过去后，显示“提醒时间已过”，不补发。

---

## 8. 结束一天：唯一的仪式功能

### 8.1 定位

**End Day 是“我先把今天放下”，而不是“我必须把清单清空”。**

不强制使用，不要求固定时刻，不要求写总结，不因一天完成零项而阻止收尾，也不要求所有未完成任务都有去向。

### 8.2 入口与结构

从今天页面主动进入，一个页面完成全部流程，不使用多步向导。

页面标题为“结束一天”，明确显示正在整理的日期。上半部分为“今天完成 N 项”，可展开查看；下半部分为当前今天列表里的未完成任务。

未完成任务默认全部“保持不变”。用户可以逐条选择：

| 选择 | 行为 |
|---|---|
| 保持不变 | 不改任何时间，明天仍可能作为此前安排/逾期任务出现 |
| 安排到明天 | 只改安排日期，有原钟点则保留；原来没有钟点就继续只有日期 |
| 选择其他日期 | 只改安排日期，钟点可以在选择面板中调整 |
| 取消安排 | 清除安排；任务重新匹配收件箱，标签与截止保留 |

可在次级菜单提供“全部安排到明天”，但只能由明确点击触发，并预览涉及的任务数量与截止冲突。它不是默认行为。

延期或取消安排**不能使已经到期的任务凭空离开今天视图**。例如任务仍然今天截止，即使放回收件箱，它仍会出现在今天。界面应如实保留，而不是为了收尾后的空屏隐藏风险。

### 8.3 提交

点击“结束今天”时，才把选定的安排调整与当天收尾记录一起提交到本地数据库；不修改截止，不把未完成标记完成，也不额外归档已完成任务。

取消流程时不提交任何预览中的调整。提交前重新读取任务状态：已经被通知按钮完成或删除的任务不能被旧页面复活；被其他入口修改过的待调整任务要提示重新确认。

提交成功显示“今天就到这里。”可附带“已完成 4 项，重新安排 2 项”等事实性摘要，不显示分数、庆祝特效或评价。

### 8.4 收尾之后

Today 保留真实列表，并显示“今天已收尾”。用户可以继续新增和处理任务；需要再次整理时可重新进入，但同一天只有一条收尾记录，重复提交只更新它。

第二天自动显示新日期，不要求“开始一天”，也不要求补做前一天的收尾。应用连续多天未打开，不补弹一串日终流程。

在午夜前进入、午夜后提交时，仍整理入口时显示的那一天；“明天”也相对于该日期计算。退出后回到真正的当前日期。

### 8.5 不应出现的行为

不自动把全部未完成顺延一天；不自动延后截止；不强制清空收件箱或今天；不锁住后续操作；不默认暂停尚未到时的提醒。收尾状态只表示用户已经做过一次整理，不表示任务系统已停用。

---

## 9. 本地通知与可靠性

### 9.1 通知内容和动作

通知包含任务标题与必要的提醒原因，例如“安排时间到了”或“即将截止”。点通知打开该任务；提供“完成”快捷动作。不在 V1 加入复杂的通知内编辑或多个稍后提醒选项。

同一任务的同一提醒实例只能通知一次。对同一任务重复发送完成操作，应保持幂等。任务完成、删除或提醒改期后，旧提醒不得继续触发。

通知的锁屏预览默认隐藏任务内容，展示通用文字“你有一项待办提醒”；用户可以在设置中允许显示标题，并尊重系统通知隐私设置。

### 9.2 权限申请

首次启动不索取通知或闹钟权限。第一次明确启用提醒时再解释并申请。

Android 13 及以上通常需要 `POST_NOTIFICATIONS` 运行时权限。拒绝后仍允许保存和管理任务，但提醒区域必须显示“通知未开启”，不能显示为已经可靠启用。[R1]

精确提醒与通知显示是两件事。需要准确钟点时采用 `SCHEDULE_EXACT_ALARM` 对应的授权流程，实际调度前检查权限；未获授权则使用系统允许的非精确调度并标注“可能延迟”。不假定所有新安装都会自动拥有精确提醒权限。[R2]

### 9.3 调度策略

采用 Android 本地 `AlarmManager` 与接收器执行一次性提醒，不依赖 Google Play Services、FCM、网络或常驻前台服务。不要用周期轮询模拟精确提醒；系统闹钟可在应用进程不运行时承担时间触发任务。[R2]

提醒配置必须保存在数据库中，系统闹钟只是可重建的执行结果。任务修改先提交数据库，再协调注册/取消系统提醒；调度失败不丢失任务，并显示“任务已保存，提醒未能启用”。

开机、应用启动/升级、时间/时区变化及相关授权恢复后，从数据库重新协调需要存在的提醒。重启后的恢复不可省略，Android 在设备关闭时会取消已注册闹钟。[R2]

接收器在发出通知前，重新确认任务仍未完成、提醒仍有效、版本与触发时间仍匹配。数据库和系统调度之间不能依靠“一次调用一定成功”的假设。

### 9.4 错过提醒

因关机或系统限制错过的有效提醒，在下一次可恢复时统一检查。V1 默认对过去 24 小时内未通知过的提醒最多补发一条汇总；更早的提醒不集中轰炸用户。已完成、已删除、已经通知过及恢复备份带来的过去提醒均不补发。

新建时输入过去日期不属于“错过提醒”。用户关闭通知后，再次开启不应补发全部历史通知。

### 9.5 能力边界

不承诺“任何情况下都准时”。强行停止应用与正常退出不同；例如 Android 15 会在应用进入 stopped 状态时取消 PendingIntent，并要求后续用户交互解除该状态。[R3]

设置里提供一个简洁的“提醒状态”区域，分别显示通知权限、通知通道和精确提醒授权。提示系统限制，不通过保活、无限循环、滥用全屏通知或常驻前台服务绕过用户与系统选择。

---

## 10. 视觉设计规范

### 10.1 方向

关键词为**安静、清楚、宽松、直接**。使用 Android 原生交互习惯，以排版与留白建立层级，而不是靠每项任务一个大卡片。

不使用渐变背景、毛玻璃、大面积阴影、装饰插画、渐变文字、拟物日历或大号生产力统计。任务是视觉中心，界面不争夺注意力。

### 10.2 初始设计变量

以下是设计默认值，不是不可调整的像素合同；调整必须保持整体一致，并在真实字体缩放下验证。

| 项目 | V1 默认 |
|---|---|
| 页面水平留白 | 20 dp，小屏可收至 16 dp |
| 间距系统 | 4 / 8 / 12 / 16 / 24 / 32 dp |
| 页面标题 | 28 sp，中等字重 |
| 任务标题 | 16 sp，正常字重 |
| 次级文字 | 13–14 sp，不使用过低对比度 |
| 普通任务行 | 单行最小 64 dp；有元信息或换行时自然增高 |
| 圆角 | 紧凑控件 10–12 dp；底部面板顶部约 24 dp |
| 图标 | 同一套轮廓风格；视觉尺寸约 20–24 dp |
| 色彩 | 中性底色、清晰文字、一种低饱和强调色 |

浅色可从暖白 `#FAFAF8`、深灰文字 `#202124`、蓝灰强调 `#405E75` 开始；深色从 `#141617`、文字 `#ECEEED`、强调 `#B3CBDC` 开始。所有最终前景/背景组合都需验证可读性。颜色值只是起点，不替代对比度测试。

支持跟随系统、浅色、深色三种外观。V1 不提供动态调色、标签配色盘或自定义字体。

### 10.3 组件一致性

收件箱、今天、搜索和标签详情复用同一种任务行；安排、截止与提醒复用同一套时间面板语言。保存、取消、删除的位置与文案在各页面保持一致。

底部导航只显示三项；主按钮不遮挡最后一项任务；键盘出现、手势导航条、状态栏与屏幕缺口都计入布局。

### 10.4 无障碍

图标可以小，触控目标不能小。可交互目标至少 48 × 48 dp，并防止相邻扩展触区重叠，这与 Android Compose 的无障碍指导一致。[R4]

所有图标动作有中文读屏名称。任务完成状态、安排、截止、提醒状态均可由 TalkBack 读出。支持大字体至 200% 而不丢失主要操作；正文不能通过强制缩小字体解决溢出。

所有核心操作有可见按钮，不能只靠手势。操作顺序与读屏焦点顺序一致；弹出/关闭面板后恢复合理焦点。

---

## 11. 动画与触感

**动画解释变化，不表演变化。**

| 场景 | 默认动效 | 参考时长 |
|---|---|---:|
| 点按按钮 | 轻量状态层变化 | 80–120 ms |
| 完成任务 | 勾选反馈后，任务行淡出并收拢；其他行平滑补位 | 180–260 ms |
| 改期离开当前列表 | 淡出与列表位置过渡 | 180–240 ms |
| 打开任务/时间面板 | 短距离滑入与淡入，收尾减速 | 220–300 ms |
| 底部导航切换 | 克制的内容淡入淡出，保留各列表位置 | 120–180 ms |
| End Day 提交成功 | 内容淡出，切入简短结束文案 | 250–350 ms |

避免高弹性回弹、任务飞出屏幕、长距离放缩、连续庆祝、点击一次触发多段仪式动画。不能为了播完动画而延迟数据保存或阻止下一次操作。

完成与结束一天可各有一次轻量触感反馈，尊重系统设置。用户关闭系统动画或选择减少动画时，应退化为直接状态变化，不保留不可跳过的完成表演。

列表项必须保持稳定身份；编辑一项不能让整个列表重新入场。批量改期只做整体稳定过渡，不让几十条任务依次播放动画。

---

## 12. 状态、文案与错误处理

| 状态 | 表现 |
|---|---|
| 首次启动 | 直接进入可用页面；不强制教程，不预填演示任务，不先申请权限 |
| 无待办 | 简短说明和添加入口，不使用“效率满分”等文案 |
| 无搜索结果 | 保留关键词，允许立即修改 |
| 保存失败 | 保留草稿，显示重试，不假装成功 |
| 提醒未授权 | 任务照常保存，提醒区域显示原因与系统设置入口 |
| 日期冲突 | 就地轻量提示，不悄悄修改用户数据 |
| 数据库无法打开 | 明确显示无法读取，不伪装成空列表，不自动清库 |
| End Day 中途退出 | 有预览调整时确认放弃，未提交的调整不写入任务 |

面向用户统一使用中文：“今天、收件箱、标签、安排、截止、提醒、结束一天”。产品名称保留 `todo`。不在普通界面混用英文技术术语。

文案中性、简短，不催促、不道德化。例如使用“还有 3 项未完成”，不使用“你今天还欠 3 项任务”。

---

### 12.1 设置页面范围

设置只保留：外观（跟随系统/浅色/深色）、减少动画、锁屏通知是否展示标题、提醒状态与系统授权入口、备份与恢复、关于与版本信息。设置页可说明本地时间语义与数据保存边界。

不增加默认工作时长、每周目标、开始工作时间、每日自动收尾、云服务或账号设置。减少动画默认跟随系统，锁屏预览默认隐藏标题。

---

## 13. 数据与实现约束

### 13.1 技术基线

V1 采用 **Kotlin + Jetpack Compose + Material 3 基础组件 + Room/SQLite**。单个 Android 应用工程即可，不建设跨平台核心、独立后端或通用任务引擎。

Room 为 SQLite 提供本地数据访问抽象、SQL 检查与迁移支持，适合作为本产品的持久化层；数据库是业务状态的唯一事实来源。[R5]

采用简单的单向状态流：界面展示状态，ViewModel 处理交互，少量数据访问与业务操作写入数据库，再由观察结果驱动界面。平台相关提醒逻辑独立封装，时间逻辑接受可替换的 Clock 以便测试。

不为每个字段建立 UseCase 类，不强制多模块 Clean Architecture，不引入远程 Repository、同步队列、插件框架、事件溯源或应用内定时循环。复杂度只用在日期一致性、数据保存和提醒恢复这些真实问题上。

构建时选择并锁定相互兼容的稳定依赖版本及稳定 SDK，记录 compileSdk、targetSdk 与测试系统版本；不使用浮动版本，不把本 spec 当成依赖版本清单。

### 13.2 最小数据模型

| 实体 | 必要字段与含义 |
|---|---|
| Task | id；title；可选 note；可选安排日期/钟点；可选截止日期/钟点；可选 completed_at 与 completed_on；created_at；updated_at；revision |
| Tag | id；name；用于去重的 normalized_name |
| TaskTag | task_id 与 tag_id，联合唯一 |
| Reminder | task_id 唯一；mode；自定义日期/钟点（仅 custom 使用）；generation；上次已投递实例标识 |
| DayClosure | date 唯一；closed_at；当时的 zone_id |

安排/截止可以用嵌入值对象表达，日期与可选钟点必须保留区分。创建、更新、完成等技术时间戳使用明确的绝对时间表示；`completed_on` 保存完成时当地日期，用于日终回看，避免更换时区后历史归属跳动。

Reminder 的实际触发时刻由模式和任务字段推导；系统闹钟注册状态不是真实任务状态。generation 用于拒绝已经作废的旧回调，不建设分布式同步版本协议。

不存储 `is_today`、`is_overdue`、`inbox=true` 或 `started_day`。这些分别是派生视图或本产品不需要的状态。

### 13.3 必须保持的不变量

一个任务在多处可见但只有一份记录；删除标签不删除任务；完成任务不改变它的截止时间；改期不改变任务身份；End Day 不改变未被用户选择调整的字段。

只有完成状态才有完成时间与完成日期，恢复时一起清除。需要钟点的提醒模式必须有合法时间源。全部日终调整与收尾记录以一个本地事务提交。

数据库迁移不能使用破坏性重建作为发布版本的默认处理方式。保存失败、提醒注册失败和读取失败必须区分，不能统称“网络错误”。

---

## 14. 本地数据保护与隐私

运行功能不使用网络，不申请网络权限，不包含广告、账号、遥测、云崩溃上报或远程配置。日志不得输出完整任务标题、备注或备份内容。

V1 不建设云备份，但在设置提供最低限度的“导出备份 / 恢复备份”。它们是本地数据保护工具，不是同步功能。

备份使用带 `schema_version` 的单个结构化文件，包含任务、标签、关联、提醒配置和收尾记录。文件是明文，导出前明确说明；通过系统文件选择器由用户选择位置，不申请管理全部文件权限。

恢复只支持**整体替换**，不做合并算法。先完整校验版本与引用，再展示任务数量并确认覆盖；用户确认前不改变现有数据。替换在事务中完成，失败保留旧数据。成功后取消旧提醒并重建未来提醒，不补发备份里的历史通知。权限状态不从备份“恢复”。

系统级备份与应用云同步要区分：Android Auto Backup 有默认参与行为，且较新系统分别配置云备份与设备转移。需要显式配置规则，排除任务数据的自动云备份，不能仅凭“App 没有网络功能”声称系统不会上传数据。[R6]

本地设备转移不作为 V1 保证的迁移途径。设置中的数据说明应写清：卸载或设备丢失可能导致本地数据丢失，手动备份文件需要自行保管。

---

## 15. 边界情况

| 情况 | 必须行为 |
|---|---|
| 任务只有截止日期，没有安排 | 留在收件箱；到期日进入今天 |
| 未完成任务跨过午夜 | 保留原日期，重新计算视图，不暗中顺延 |
| 明天安排，但今天截止 | 今天仍可见，并提示安排晚于截止 |
| 结束一天但所有任务保持不变 | 允许结束，任务照常显示 |
| 结束后再次添加今天的任务 | 允许，不要求重新开启一天 |
| 连续几天没有打开 App | 显示当前日期；不要求补做日终流程 |
| 日终页面打开时，从通知完成任务 | 提交不能覆盖完成状态或复活任务 |
| 删除标签，关联有很多任务 | 只删除关联；所有任务可在其他视图找到 |
| 修改安排，提醒跟随安排 | 旧提醒作废，新提醒按新时间注册 |
| 修改安排，提醒为独立时间 | 独立时间保留，编辑界面明确提示 |
| 通知权限或精确闹钟授权被撤销 | 不崩溃，不显示虚假成功；下次可运行时协调状态 |
| 系统重启或升级应用 | 已保存任务不变，重建仍有效的未来提醒 |
| 时区/系统时间变化 | 按本地时间语义重算列表与提醒，不重复发出同一实例 |
| 夏令时导致选择的钟点不存在 | 调整至随后第一个有效本地时刻并显示实际提醒时间 |
| 夏令时导致一个钟点出现两次 | 使用第一次，只投递一次 |
| 连续点击完成、保存、结束今天 | 幂等，不重复创建任务、记录或通知 |
| 备份损坏或版本不兼容 | 明确拒绝，现有数据库保持不变 |

---

## 16. 质量目标与验收

### 16.1 体验目标

以下是开发验收目标，不是当前已经测得的性能。用 release 构建，在记录了机型、系统版本、数据量的代表性中端 Android 真机上测量。

| 项目 | 目标 |
|---|---|
| 冷启动到可操作列表 | 典型本地数据下不超过约 1 秒 |
| 点按到可见反馈 | 通常不超过 100 ms |
| 搜索 | 10,000 条任务内，停止输入到结果更新目标不超过 300 ms |
| 列表滚动与改期动画 | 测试轨迹不出现可见长停顿；提供实际帧时间记录 |
| 离线 | 飞行模式下，全部核心工作流与已授权本地提醒仍可测试 |
| 常驻行为 | 无无意义后台轮询、常驻服务或永久通知 |

若不能达标，优先减少布局开销、主线程工作与无效动画，不通过删数据、限制核心语义或隐瞒失败达标。

### 16.2 功能验收矩阵

| ID | 测试 | 通过标准 |
|---|---|---|
| F01 | 新安装后打开 | 不登录、不联网、不先授权即可创建任务 |
| F02 | 只输入标题保存 | 其他字段可空，任务持久化成功 |
| F03 | 添加标签 | 未安排任务仍留在收件箱 |
| F04 | 安排到今天/明天 | 分别进入今天/之后，原任务 ID 不变 |
| F05 | 只有今天截止 | 同时出现在今天和收件箱，每个列表内无重复 |
| F06 | 过去安排但未来截止 | 标为此前安排，不标为逾期 |
| F07 | 日期截止与时刻截止 | 全天边界与具体时刻边界分别正确 |
| F08 | 改期晚于截止 | 警告但不自动改截止 |
| F09 | 完成与撤销 | 全部视图同步，提醒按规则取消或恢复 |
| F10 | 标签删除与任务删除 | 前者保留任务；后者须确认并取消提醒 |
| F11 | 搜索跨多个标签 | 一条任务只出现一次，包含已完成开关有效 |
| F12 | 未做 End Day 就进入次日 | 应用照常使用，无 Start Day、无强制补流程 |
| F13 | End Day 全部保持不变 | 可以成功收尾，不隐藏未完成与逾期任务 |
| F14 | End Day 预览后取消 | 所有任务日期不变 |
| F15 | End Day 明确改期并提交 | 只改选择的安排与相应提醒，不改截止 |
| F16 | End Day 后继续操作/重复收尾 | 可以继续使用；同日收尾记录不重复 |
| F17 | 结束页面跨午夜、遇到外部完成 | 整理日期不漂移，完成状态不被覆盖 |
| N01 | 拒绝通知权限 | 任务可保存，提醒状态明确提示受限 |
| N02 | 缺少精确闹钟授权 | 无异常退出，说明可能延迟 |
| N03 | App 不在前台时提醒 | 满足系统权限条件时可收到本地通知 |
| N04 | 完成/删除/改期后等待旧时间 | 不发送已经失效的提醒 |
| N05 | 重启和时间/时区变化 | 未来提醒重建，不重复通知 |
| N06 | 强行停止后再次打开 | 恢复调度并体现平台限制，不宣称期间绝对准时 |
| N07 | 延迟恢复及旧备份 | 不刷屏补发历史提醒 |
| U01 | 浅色、深色、200% 字体 | 无关键控件遮挡、截断或不可达 |
| U02 | TalkBack、关闭动画 | 核心流程完整可用，动画不作为信息唯一载体 |
| U03 | 快速连续操作与列表重排 | 不重复创建，不错选，不整页闪烁 |
| D01 | 杀进程重开、数据库迁移 | 已保存任务和提醒配置不丢失 |
| D02 | 保存/调度/读取失败注入 | 分别提示，保留用户可恢复的数据 |
| D03 | 导出后恢复、损坏备份 | 正常备份可恢复；损坏文件不覆盖原数据 |

至少在一台无 Google Play Services 的设备环境验证离线功能与本地提醒，不把在带 Google 服务的模拟器通过视为全部验证。

---

## 17. 实施顺序与交付

| 阶段 | 内容 | 完成判据 |
|---|---|---|
| A：正确的任务模型 | 本地存储、收件箱/今天/标签、日期语义、基本增删改完成 | 真实数据驱动界面；日期与视图测试通过 |
| B：可靠的提醒 | 单次提醒、权限状态、改期取消、重启恢复、通知完成动作 | 真机覆盖后台、撤权、重启与过期提醒 |
| C：收尾与数据保护 | End Day、已完成、搜索、备份恢复 | 收尾无强制行为，无隐性数据改写 |
| D：视觉与交互完成度 | 统一设计变量、暗色、键盘、安全区、动效、无障碍 | 用真实状态与长列表逐屏验收 |

设计变量在 A 阶段就建立，不要先堆一套默认组件，最后把“简单大气”理解为改颜色。动画在逻辑稳定后补齐。

交付物应包括：完整源码；可安装的 APK；可重复构建说明；日期与任务规则单元测试；关键 UI 测试；通知场景的真机测试记录；浅色/深色关键页面截图；一段展示添加、完成、改期与结束一天的操作录屏；已知限制与未通过项。

不能只交付静态界面、假数据 Demo 或假通知。未经实际测试的项目必须标记“未验证”，不得写成已通过。

### 给实现 agent 的最终约束

实现的是本 spec 中的 **todo V1**，不是缩小版的 Super Productivity。只借鉴收集、安排、提醒与日终收尾的工作流，不复制其全部功能与产品架构。

不得擅自添加跨端同步、账号、项目层级、优先级、重复任务、时间估算、计时、AI、统计、Start Day 或强制日终清零。遇到规格冲突，优先保存用户数据、保持时间语义、避免隐性自动操作，并记录取舍。

**成功标准：想到的事能立即记下，安排的事能清楚看到，需要提醒时可靠且透明，一天结束时可以轻松放下。**

---

## 平台依据

以下来源用于核对 Android 平台约束，不表示本 spec 的产品取舍来自官方规定。查阅日期：2026-09-10。

- [R1] Android Developers — Notification runtime permission: https://developer.android.com/develop/ui/compose/notifications/notification-permission
- [R2] Android Developers — Schedule alarms: https://developer.android.com/develop/background-work/services/alarms
- [R3] Android Developers — Android 15 behavior changes, package stopped state: https://developer.android.com/about/versions/15/behavior-changes-all
- [R4] Android Developers — Compose accessibility API defaults: https://developer.android.com/develop/ui/compose/accessibility/api-defaults
- [R5] Android Developers — Save data in a local database using Room: https://developer.android.com/training/data-storage/room
- [R6] Android Developers — Back up user data with Auto Backup: https://developer.android.com/identity/data/autobackup
