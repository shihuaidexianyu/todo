# 1.2.0 验证记录

测试环境：Windows、Pixel 9a Android 17 / API 37 模拟器。滑动手感与震动强度仍需真机确认。

| 检查 | 结果 |
|---|---|
| Release APK / AAB | 构建成功，versionCode 6 |
| JVM 单元测试 | 19 项通过（Rules 日期与排序语义） |
| 模拟器端测试 | 23 项通过：界面工作流 9、数据层 10、提醒平台 2、分享与快捷方式 2 |
| Release Lint | 0 错误、31 警告 |
| 覆盖安装 | 1.1.2 → 1.2.0，同签名升级成功 |
| 手势与创建页 | 模拟器截图核对：圆形悬浮徽章、庆祝空态、近全屏编辑器 |

测试期间模拟器两次出现系统进程无响应（System UI / system_server ANR），冷启动后重跑全绿，与应用改动无关。

原始构建与操作证据保留在本地 `artifacts/release-validation/1.2.0/`。发布包校验和随 Release 提供。

---

# 1.1.2 验证记录

测试环境：Pixel 9a Android 17 / API 37 模拟器。声音的实际听感、震动强度仍需真机确认。

| 检查 | 结果 |
|---|---|
| Release APK / AAB | 构建成功，versionCode 5 |
| JVM 单元测试 | 28 项通过 |
| 针对性界面回归 | 3 项通过：完成恢复与收尾、混合日期导航、反馈设置持久化 |
| Release Lint | 0 错误、30 警告 |
| 覆盖安装 | 1.1.1 → 1.1.2，同签名升级 |
| Release 操作回归 | 创建备注、收起与恢复今天、完成与恢复、底部导航 |
| 音效文件 | 单声道 PCM 48 kHz、165 ms、峰值 -2.7 dBFS，无削波 |

首次针对性测试受到模拟器 System UI 无响应弹窗及窗口重建干扰，后两项未找到 Compose 界面；关闭系统弹窗后重新运行这两项。

本次未重跑全量设备测试。1.1.1 的旋转自动化等待问题仍未确认解决；本次重点验证完成动效不会改变任务数据或破坏恢复与导航。

原始构建与操作证据保留在本地 `artifacts/release-validation/1.1.2/`。发布包校验和随 Release 提供。

---

# 1.1.1 验证记录

测试环境：Windows、Pixel 9a Android 17 / API 37 模拟器。以下结果不代表已覆盖不同厂商的真机与输入法，震动强度与实际听感需在真机确认。

| 检查 | 结果 |
|---|---|
| Release APK / AAB | 构建成功，versionCode 4 |
| JVM 单元测试 | 28 项通过 |
| Compose 界面工作流 | 6 项通过 |
| Release Lint | 0 错误、30 警告 |
| 1.1.0 → 1.1.1 | 同签名覆盖安装成功 |
| 最终 Release UI 回归 | 通过；应用崩溃缓冲区为空 |

界面工作流覆盖：创建与今天截止、标签当页展开、备注标记和预览、结束一天前后任务记录一致、重建后保留收起状态、其他页面访问任务、重新展开今天、深色模式、设置和已完成往返导航，以及反馈开关重建后的持久化。

最终 release 另行检查了备注创建与展示、收起后重启、标签中访问隐藏任务、重新展开、完成和恢复无提示条、底部已完成入口及直接进入设置。截图见 README。

界面测试前四项通过后，200% 字体旋转测试在横屏等待环节未结束，已中止该轮并单独运行剩余两项完成与导航测试。横屏截图已检查，布局可用，但这项自动化不计入通过数。尚未覆盖 Android 10 真机、厂商后台限制以及各类第三方输入法。

原始构建、测试、签名和截图证据保留在本地 `artifacts/release-validation/1.1.1/`。发布包的校验和随 GitHub Release 提供。
