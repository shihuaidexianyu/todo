# 1.1.0 验证记录

测试环境：Windows、Pixel 9a Android 17 / API 37 模拟器。以下结果不代表已覆盖不同厂商的真机与输入法。

| 检查 | 结果 |
|---|---|
| Release APK / AAB | 构建成功，versionCode 3 |
| JVM 单元测试 | 28 项通过 |
| Compose 界面工作流 | 6 项通过 |
| Release Lint | 0 错误、28 警告 |
| 1.0.1 → 1.1.0 | 同签名覆盖安装成功 |
| 最终 Release UI 回归 | 通过；应用崩溃缓冲区为空 |

界面工作流覆盖：创建与今天截止、标签当页展开、备注标记和预览、结束一天前后任务记录一致、重建后保留收起状态、其他页面访问任务、重新展开今天、深色模式、200% 字体与横屏、设置和已完成往返导航。

最终 release 另行检查了备注创建与展示、收起后重启、标签中访问隐藏任务、重新展开、直接进入设置及已完成页面。截图见 README。

模拟器在早期测试中出现过 System UI 无响应弹窗，关闭后完成最终 release 回归。尚未覆盖 Android 10 真机、厂商后台限制以及各类第三方输入法。

原始构建、测试、签名和截图证据保留在本地 `artifacts/release-validation/1.1.0/`。发布包的校验和随 GitHub Release 提供。
